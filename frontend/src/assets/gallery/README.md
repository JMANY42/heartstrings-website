# Gallery images

Drop images here named `image_1`, `image_2`, ... `image_n`.

Supported extensions: `.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`, `.gif`.
They are picked up automatically at build time (see `src/components/Gallery.tsx`)
and sorted numerically by the trailing number, so `image_10` follows `image_9`.
